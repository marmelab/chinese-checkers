{% extends "base.html.twig" %}

{% block body %}
	<main>
		<twig:Board board="{{ board }}" winner="{{ winner }}" />
	</main>
	<aside>
		<twig:Winner winner="{{ winner }}" />

		<twig:Flash />
		<twig:Turn />
	</aside>
{% endblock %}
